apiVersion: v1
kind: Service
metadata:
  name: rest-server-service
spec:
  type: ClusterIP  # Use NodePort or LoadBalancer to expose outside the cluster
  selector:
    app: rest-server
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
